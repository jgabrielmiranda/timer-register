plugins { // Remove this plugin because have no idea what it is and why it was added here
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}

rootProject.name = "TimerRegister"